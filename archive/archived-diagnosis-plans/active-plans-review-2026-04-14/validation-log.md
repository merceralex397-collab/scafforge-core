# Validation Log

## Scafforge Fixes Validated

| Fix | Validation Method | Result | Date |
|-----|------------------|--------|------|
| QA-artifact smoke-command inference for stale remediation tickets (working tree) | `npm run validate:contract`; `npm run validate:smoke`; live spinner evidence from `active-plans/agent-logs/spinner-opencode-2026-04-13T02-56-40.log` plus direct artifact inspection of `spinner/.opencode/state/artifacts/history/remed-042/qa/2026-04-13T02-00-53-206Z-qa.md` and `.../smoke-test/2026-04-13T02-02-52-296Z-smoke-test.md` | The generated `smoke_test` tool now infers deterministic smoke commands from the current QA artifact when no explicit override or acceptance smoke command is present, the new regression proves a JSON `Commands Executed` QA artifact drives a real Godot headless smoke run successfully, and package validation is green again. | 2026-04-13 |
| Clearable process-verification routing (working tree) | `npm run validate:contract`; `npm run validate:smoke`; live spinner evidence from `active-plans/agent-logs/spinner-opencode-2026-04-13T02-07-14.log` plus direct workflow-state inspection showing `pending_process_verification: true` with zero affected done tickets | The generated `ticket_lookup` contract now routes stale clearable process-verification cleanup through `ticket_update` on the current open writable ticket instead of only on closed tickets, the new smoke regression proves both the closed-ticket and already-rotated open-ticket paths, and package validation is green again. | 2026-04-13 |
| Closeout status contract hardening (working tree) | `npm run validate:contract`; `npm run validate:smoke`; synced updated `repo-scaffold-factory` and `scafforge-audit` skill folders to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real `./scripts/run_agent.sh spinner --repair`; verified refreshed spinner repo-local `.opencode/tools/ticket_lookup.ts` and `.opencode/agents/spinner-team-leader.md` | The generated guidance now emits `stage: "closeout", status: "done"` for closeout transitions, the template/generated prompts explicitly spell out `closeout -> done`, package validation is green, and managed repair propagated the new contract into spinner’s repo-local surfaces while keeping `repair_follow_on.outcome: source_follow_up`. | 2026-04-13 |
| Delegation-boundary persistence + recap suppression hardening (working tree) | `npm run validate:contract`; `npm run validate:smoke`; inspected live spinner run `active-plans/agent-logs/spinner-opencode-2026-04-13T01-15-12.log` for delegation-boundary early exit evidence | The team-leader template, generated scaffold, and host resume prompt now all require specialist delegation to stay in-band until the result is consumed, and forbid repetitive long recap blocks during routine progress. Package validation is green again with explicit contract checks for both rules. | 2026-04-13 |
| Inline remediation review-output compatibility (working tree) | Targeted generated-repo repro for `## Verdict: **APPROVE**` + `**Exact command run**` + `**Raw output**` + `**Result: PASS**`; `npm run validate:contract`; `npm run validate:smoke` | Runtime now classifies the inline artifact as `APPROVED`, `ticket_update` allows review→QA, public audit stays quiet on the same artifact shape, and the full package validation suite is green again. | 2026-04-13 |
| Remediation-only review-evidence scoping (working tree) | `npm run validate:contract`; `npm run validate:smoke`; added runtime + audit regressions covering both remediation and non-remediation `finding_source` review tickets | The shared gate now enforces strict rerun-command review evidence only for true remediation tickets, while non-remediation follow-up tickets that carry `finding_source` can still advance from review to QA on an approved artifact. Package validation is green with both regression families locked in. | 2026-04-13 |
| hasPendingRepairFollowOn import (686cc74a) | Repair + agent launch on all 3 repos | Tool calls work | 2026-04-09 |
| Copytree .venv exclusion (6e9bb3dc) | Repair runs on GPTTalker (has .venv) | No crash | 2026-04-09 |
| Model prefix stripping (7ead92e0) | Inspect rendered opencode.jsonc | Correct model string | 2026-04-09 |
| Provenance round-trip (03adb917) | Inspect rendered surfaces after repair | Correct stack labels | 2026-04-09 |
| REMED-RELEASE-GATE idempotency (c8bb368f) | Re-run repair on spinner/glitch | No crash | 2026-04-09 |
| managed_blocked error message (9460cc63) | Glitch agent self-unblocked immediately | repair_follow_on_refresh used | 2026-04-09 |
| RC-001 managed_blocked enforcement (906dc002) | GPTTalker agent blocked correctly | BLOCKER thrown | 2026-04-09 |
| RC-002 deterministic-refresh (906dc002) | Inspect workflow state after repair | No polluted stages | 2026-04-09 |
| CONFIG audit (c7546b95) | Audit runs on all 3 repos | Findings generated | 2026-04-09 |
| Headless audit/repair fallback policy (working tree) | `bash -n scripts/run_agent.sh`; dry-run audit/repair invocations for auto, codex, and kilo lanes; `npm run validate:contract` | Runner syntax valid, provider order rendered correctly, contract validation passed | 2026-04-12 |
| Package-root-safe audit/repair skill commands (working tree) | Real `./scripts/run_agent.sh wvhva --audit`; updated skill text; rerun `npm run validate:contract` | Real audit succeeded through the host runner, skill command docs now match package-root execution, contract validation passed again | 2026-04-12 |
| GPTTalker audit via repaired host lane (working tree) | Real `./scripts/run_agent.sh gpttalker --audit`; inspected emitted diagnosis pack `GPTTalker/diagnosis/20260412-020329/` | Codex remained primary with medium reasoning; package-root audit skill text was used; fresh managed blockers remain `WFLOW010` and `WFLOW019`, with `EXEC-REMED-001` still widespread | 2026-04-12 |
| GPTTalker remediation-loop hardening (working tree) | Revalidated with `npm run validate:contract` + `npm run validate:smoke`; ran fresh audit → repair → fresh audit on GPTTalker (`20260412-023736`, `repair-execution.json` with diagnosis `20260412-025214`, final public audit `20260412-025410`) | Public GPTTalker audit now shows only 8 × `EXEC-REMED-001`; `WFLOW019`, `CYCLE002`, and public-audit `EXEC001` are gone; repair follow-up explicitly skips candidate-only temp-surface `EXEC001` and supersedes stale temp-surface remediation tickets | 2026-04-12 |
| Spinner / womanvshorseVA workflow confirmation (working tree) | Real fresh audits on `spinner` and `womanvshorseVA`, plus managed repair + public audit on `spinner` (`spinner/diagnosis/20260412-025652`, `20260412-030056`, `20260412-030215`; `womanvshorseVA/diagnosis/20260412-025704`) | `spinner` drops from `WFLOW010` + `WFLOW019` + 19 × `EXEC-REMED-001` to 19 × `EXEC-REMED-001` only after repair, and the public audit stays clean of workflow findings; `womanvshorseVA` fresh audit shows only 2 × `EXEC-REMED-001` with no surviving `WFLOW019` | 2026-04-12 |
| Auto fallback loop + remaining workflow-drift repos (working tree) | `bash -n scripts/run_agent.sh`; traced broken auto-fallback under Codex quota exhaustion; patched provider-loop control flow; reran real audits/repairs on `glitch`, `womanvshorseVB`, `womanvshorseVC`, `womanvshorseVD`, then reran public audits through Kilo fallback (`agent-logs/*04-15-11.log`, `*04-16-27.log`) | Auto fallback now advances from Codex rate-limit failure into successful Kilo execution. `glitch`, `womanvshorseVB`, `womanvshorseVC`, and `womanvshorseVD` public audits no longer show Scafforge-routed workflow blockers; remaining findings are source-follow-up / product-surface only | 2026-04-12 |
| False-finish smoke contradiction hardening (working tree) | `npm run validate:contract`; `npm run validate:smoke`; synced updated `scafforge-audit` and `repo-scaffold-factory` skill folders to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real `./scripts/run_agent.sh wvhva --audit` through Kilo fallback | Package validators pass with the stricter smoke behavior, and fresh `womanvshorseVA` audit `diagnosis/20260412-033025/` now emits `EXEC-GODOT-006` from the retained PASS artifact that records Godot parse/load failures. The old false-finish proof shape is now visible to public audit instead of slipping through as follow-up-only noise. | 2026-04-12 |
| Finish-validation lane backfill for consumer repos (working tree) | `npm run validate:contract`; `npm run validate:smoke`; synced updated `scafforge-audit`, `scafforge-repair`, and `repo-scaffold-factory` skill folders to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real `./scripts/run_agent.sh wvhva --audit`; real `./scripts/run_agent.sh wvhva --repair`; real follow-up `./scripts/run_agent.sh wvhva --audit`; real `./scripts/run_agent.sh spinner --audit`; real `./scripts/run_agent.sh spinner --repair`; real follow-up `./scripts/run_agent.sh spinner --audit` | Fresh VA public audit `diagnosis/20260412-034402/` and fresh spinner public audit `diagnosis/20260412-034826/` both emit `FINISH003` before repair. Managed repair backfills the finish-validation lane into both repos (`FINISH-VALIDATE-001`, plus missing finish ownership tickets where needed). Follow-up public audits `womanvshorseVA/diagnosis/20260412-034655/` and `spinner/diagnosis/20260412-035103/` both clear `FINISH003`, leaving only downstream source-follow-up/product findings. | 2026-04-12 |
| Weak interactive finish-contract hardening (working tree) | `npm run validate:contract`; `npm run validate:smoke`; synced updated `scafforge-audit`, `scafforge-repair`, and `repo-scaffold-factory` skill folders to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real `./scripts/run_agent.sh spinner --audit`; attempted `./scripts/run_agent.sh spinner --repair` (Codex quota exhausted, Kilo stalled before mutation); successful `./scripts/run_agent.sh spinner --repair --provider copilot`; real follow-up `./scripts/run_agent.sh spinner --audit` | Fresh public spinner audit `diagnosis/20260412-040413/` emits `FINISH004` for the old weak generated finish contract. Copilot-backed managed repair upgrades the brief and `FINISH-VALIDATE-001`, records the required `project-skill-bootstrap` and `ticket-pack-builder` follow-on stages for the current cycle, and keeps verification on the same cycle (`diagnosis/20260412-041729/`). Fresh public audit `spinner/diagnosis/20260412-041905/` clears `FINISH004`, `FINISH003`, and `SKILL001`, leaving only `EXEC-REMED-001` source-follow-up findings. | 2026-04-12 |
| Blender-agent validation-contract hardening (working tree) | Real host Blender probe via `BLENDER_MCP_BLENDER_EXECUTABLE=/home/pc/blender-4.5.0/blender`; `uv sync --extra test`; `uv run python -m unittest tests.test_config tests.test_live_session tests.test_runtime_worker_persistence -v`; `BLENDER_MCP_BLENDER_EXECUTABLE=/home/pc/blender-4.5.0/blender uv run python -m unittest tests.test_blender_integration -v`; `uv run python -m pytest tests/test_schema_redesign.py tests/test_watch_mode.py -q`; `uv run python -m pytest tests -q` | Real Blender-backed integration continues to pass on this host, the config/live-session/runtime-persistence regressions are fixed, schema validation and watch-mode transport expectations now match the code, and the documented full-suite command converges with the actual repo surface (`372 passed, 22 skipped`). | 2026-04-12 |
| Remediation-review verdict + Kilo repair recursion hardening (working tree) | `npm run validate:contract`; `npm run validate:smoke`; synced updated `scafforge-repair` skill folder to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real auto `./scripts/run_agent.sh spinner --repair` under Codex exhaustion; real follow-up `./scripts/run_agent.sh spinner --continue` | The repaired contract now accepts markdown remediation-review verdict sections that previously blocked spinner review→QA transitions, and Kilo repair stays on `run_managed_repair.py` instead of recursing into `run_agent.sh --repair`. Fresh auto repair log `spinner-repair-2026-04-12T06-06-24.log` records both required follow-on stages and reconciles to `source_follow_up`; the follow-up live run `spinner-opencode-2026-04-12T06-08-36.log` closes `REMED-021` and advances cleanly into `REMED-022`. | 2026-04-12 |
| Session-backed coordinator-artifact enforcement (working tree) | `npm run validate:contract`; `npm run validate:smoke`; synced updated `repo-scaffold-factory` skill folder to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real `./scripts/run_agent.sh spinner --repair --provider copilot`; real follow-up `./scripts/run_agent.sh spinner --continue` | The stage gate now resolves the active agent from `invocation-log.jsonl` when live tool hooks arrive with `agent: null`, so coordinator-authored planning/implementation/review/QA artifacts are blocked in the same runtime shape spinner actually emits. Fresh live run `spinner-opencode-2026-04-12T07-03-46.log` shows the repaired repo rejecting coordinator planning writes with `Coordinator-authored planning artifacts are not allowed.` and then routing `REMED-026` through `spinner-planner` / `spinner-implementer` instead. | 2026-04-12 |
| QA verdict-family parser hardening (working tree) | `npm run validate:contract`; `npm run validate:smoke`; inspected live spinner blocker in `active-plans/agent-logs/spinner-opencode-2026-04-12T07-03-46.log`; inspected current downstream QA artifacts (`spinner/.opencode/state/qa/remed-014-qa-qa.md`, `spinner/.opencode/state/qa/remed-027-qa-qa.md`, `womanvshorseVD/.opencode/state/qa/remed-002-qa-qa.md`); synced updated `scafforge-audit` and `repo-scaffold-factory` skill folders to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/` | The shared verdict extractor now accepts compact QA/review headings and plain `Overall` labels that already exist in live downstream artifacts, while public audit flags the same family under `WFLOW026`. Local package validation passes with new regression coverage for `## QA PASS` and `**Overall**: PASS`, so the spinner blocker that rejected explicit QA PASS artifacts is now fixed in the package. | 2026-04-12 |
| WFLOW026 post-package routing repair (working tree) | Direct `python3` probes of `package_has_verdict_parser_fix(...)` and `build_ticket_recommendations(...)`; `npm run validate:contract`; `npm run validate:smoke` | The audit recommendation layer now recognizes the widened verdict parser as an installed package fix, and the smoke suite stays green after asserting `WFLOW026` routes to `scafforge-repair` through the real recommendation builder instead of remaining stuck as `manual-prerequisite`. | 2026-04-12 |
| WFLOW026 audit false-positive elimination + spinner revalidation (working tree) | Legacy-vs-current parser probe harness; `npm run validate:contract`; `npm run validate:smoke`; synced updated `scafforge-audit` skill to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real `./scripts/run_agent.sh spinner --audit`; real `./scripts/run_agent.sh spinner --repair`; real `./scripts/run_agent.sh spinner --continue` | Fresh spinner public audit `diagnosis/20260412-220721/` drops `WFLOW026` completely and contains only 30 × `EXEC-REMED-001` source follow-up findings. The follow-up repair cycle from that diagnosis (`repair-execution.json` with basis `20260412-220721`, plus post-repair packs `20260412-221355/` and `20260412-221550/`) reconciles `repair_follow_on.outcome` to `source_follow_up`, clears `verification_blocking_codes`, and allows the resumed team-leader run `spinner-opencode-2026-04-12T23-17-25.log` to continue into normal child-ticket activation (`REMED-030`) instead of re-trapping on the old `managed_blocked` / `WFLOW026` loop. | 2026-04-12 |
| Headless team-leader persistence hardening (working tree) | `npm run validate:contract`; `npm run validate:smoke`; synced updated `repo-scaffold-factory` skill folder to `~/.codex/skills/`, `~/.copilot/skills/`, and `~/.config/kilo/skills/`; real `./scripts/run_agent.sh spinner --repair`; real follow-up `./scripts/run_agent.sh spinner --continue` | The repaired team-leader prompt now forbids stopping on self-addressed `"Next Steps"` summaries while legal workflow actions remain. After managed repair refreshed `spinner/.opencode/agents/spinner-team-leader.md`, the live resumed run `spinner-opencode-2026-04-12T23-47-55.log` carried `REMED-029` all the way through implementation, review, QA, smoke-test, closeout, and lease release in one headless run, then immediately foregrounded `REMED-034` instead of exiting after an internal handoff summary. | 2026-04-12 |

## Downstream Agent Execution

### GPTTalker Agent Runs

| Run | Time | Outcome | Key Events |
|-----|------|---------|------------|
| Run 1 (pre-import-fix) | 13:29 | FAILED | hasPendingRepairFollowOn undefined |
| Run 2 (post-import-fix) | 13:29 | FAILED | managed_blocked — old error message |
| Run 3 (improved message) | 13:36 | IN PROGRESS | Unblocked, identified migration bug, delegating to implementer |

### Spinner Agent Runs

| Run | Time | Outcome | Key Events |
|-----|------|---------|------------|
| Run 1 (pre-import-fix) | 13:29 | FAILED | hasPendingRepairFollowOn undefined |
| Run 2 (improved message) | 13:36 | IN PROGRESS | Self-unblocked, attempting Godot export |

### Glitch Agent Runs

| Run | Time | Outcome | Key Events |
|-----|------|---------|------------|
| Run 1 (pre-import-fix) | 13:29 | FAILED | hasPendingRepairFollowOn undefined |
| Run 2 (improved message) | 13:37 | IN PROGRESS | Self-unblocked via repair_follow_on_refresh, working on REMED-004 |
