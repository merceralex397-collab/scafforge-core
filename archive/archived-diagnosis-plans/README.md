> **REFERENCE:** Historical context only. Current package work is governed by the `recovery-plan/` directory.

# Archived Diagnosis Plans

This directory preserves the small set of historical Scafforge diagnosis and remediation planning notes that were previously buried inside `out/scafforge audit archive/Archived Plans/`.

The bulk audit logs, churn exports, and raw session dumps were intentionally removed from the product tree and replaced with curated fixtures under `tests/fixtures/gpttalker/`.
