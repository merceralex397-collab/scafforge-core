# Documentation Authority Map

This plan artifact now points at the stable package reference:

- [references/documentation-authority-map.md](../../../references/documentation-authority-map.md)

Plan `11` produced the authority map during the sweep, but the durable routing surface now lives under `references/` so root docs do not depend on a plan-local path that will later move to archive.
