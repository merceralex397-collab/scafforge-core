# Agent Caller Prompts

## planchecker
{{COMMON_PLANCHECKER}}

Plan-specific focus:
- verify the plan drives Meta-Skill-Engineering toward a genuinely complete CLI/control surface
- check that useful `plugin-eval` techniques are intentionally adapted or rejected with reasons
- confirm the plan treats WPF, Studio, scripts, and headless automation as one aligned suite contract

## planimplementer
{{COMMON_PLANIMPLEMENTER}}

Plan-specific focus:
- implement the feature inventory, full CLI contract, structured evaluation artifacts, and authoritative-surface alignment in the adjacent repo
- make headless agent use first-class without letting WPF become the only real product path
- adapt the useful `plugin-eval` techniques that improve evaluation quality, comparison, and improvement-brief generation

## planprreviewer methodology
{{COMMON_PR_REVIEW_METHODOLOGY}}

Plan-specific review focus:
- check that all core workflows are actually reachable from CLI or documented automation, not just from UI surfaces
- verify evaluation outputs are more structured than prose and that imported `plugin-eval` ideas are scoped sensibly
- look for contradictions between Studio, scripts, WPF, and root documentation

## planprreviewer big-pickle
Review the supplied PR context for PR #{{PR_NUMBER}} in {{OWNER_REPO}}, then return one complete top-level PR comment body for agent-caller to post.
Model emphasis: platform-level workflow gaps, incomplete CLI coverage, and hidden dependence on interactive surfaces.

## planprreviewer minimax-m2.7
Review the supplied PR context for PR #{{PR_NUMBER}} in {{OWNER_REPO}}, then return one complete top-level PR comment body for agent-caller to post.
Model emphasis: lifecycle coherence across create, improve, test/evaluate, benchmark, catalog, and packaging flows.

## planprreviewer devstral-2512
Review the supplied PR context for PR #{{PR_NUMBER}} in {{OWNER_REPO}}, then return one complete top-level PR comment body for agent-caller to post.
Model emphasis: specific CLI/action gaps, JSON-artifact issues, benchmark harness defects, and automation coverage holes.

## planprreviewer mistral-large-latest
Review the supplied PR context for PR #{{PR_NUMBER}} in {{OWNER_REPO}}, then return one complete top-level PR comment body for agent-caller to post.
Model emphasis: conceptual alignment between docs, evaluation methodology, suite terminology, and authoritative execution paths.

## planprreviewer kimi-k2.5-turbo
Review the supplied PR context for PR #{{PR_NUMBER}} in {{OWNER_REPO}}, then return one complete top-level PR comment body for agent-caller to post.
Model emphasis: real operator and agent ergonomics, especially whether the suite is practical to run headlessly and iteratively.



