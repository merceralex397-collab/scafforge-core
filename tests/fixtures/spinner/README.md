# spinner Fixture Corpus

These fixtures encode the first-screen and viewport truth failures documented in the spinner planning notes.

Use `tests/fixtures/spinner/index.json` as the canonical corpus contract.
