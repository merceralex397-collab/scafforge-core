# visual-proof Fixture Corpus

These fixtures encode visually broken but technically runnable states that should stay blocked until structured visual proof is present.

Use `tests/fixtures/visual-proof/index.json` as the canonical corpus contract.
